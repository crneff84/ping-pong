//BACKEND





//FRONTEND
$(document).ready(function(){
  $("#blanks form").submit(function(event) {
  event.preventDefault();
  var userInput = parseInt($("#number-input").val());
  var output =
  $("#list-output").empty().append("<li>"+ output + "</li>");

  });
});
